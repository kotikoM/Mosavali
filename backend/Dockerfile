FROM python:3.12-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

CMD ["sh", "-c", "python -c 'from app.migrator import run_migrations; run_migrations()' && uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload"]